<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Members</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 text-gray-900">

    <div class="max-w-5xl mx-auto p-6 mt-10 bg-white shadow-lg rounded-lg">
        <h1 class="text-3xl font-bold text-center text-indigo-600 mb-6">Manage Members</h1>

        <table class="w-full border-collapse border border-gray-300">
            <thead class="bg-indigo-600 text-white">
                <tr>
                    <th class="p-3">#</th>
                    <th class="p-3">Name</th>
                    <th class="p-3">Email</th>
                    <th class="p-3">Year of Birth</th>
                    <th class="p-3">Gender</th>
                    <th class="p-3">Role</th>
                </tr>
            </thead>
            <tbody>
                <% if (members.length> 0) { %>
                    <% members.forEach((member, index)=> { %>
                        <tr class="border-b border-gray-300 text-center hover:bg-gray-100 transition">
                            <td class="p-3">
                                <%= index + 1 %>
                            </td>
                            <td class="p-3 font-semibold">
                                <%= member.name %>
                            </td>
                            <td class="p-3 text-gray-600">
                                <%= member.email %>
                            </td>
                            <td class="p-3">
                                <%= member.YOB %>
                            </td>
                            <td class="p-3">
                                <%= member.gender ? "Male" : "Female" %>
                            </td>
                            <td class="p-3">
                                <span
                                    class="<%= member.isAdmin ? 'bg-green-500' : 'bg-gray-400' %> text-white px-2 py-1 rounded">
                                    <%= member.isAdmin ? "Admin" : "User" %>
                                </span>
                            </td>
                        </tr>
                        <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="7" class="p-5 text-center text-gray-500">No members found.</td>
                                </tr>
                                <% } %>
            </tbody>
        </table>

    </div>

</body>

</html>